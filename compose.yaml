services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      AEMET_API_KEY: ${AEMET_API_KEY}

  frontend:
    build: ./frontend
    ports:
      - "4200:80"
    depends_on:
      - backend
